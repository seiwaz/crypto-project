# Coins to screen — one per line.
#
# Write the plain ticker, not a Nobitex symbol: BTC, not BTCUSDT. Discovery works out
# the real market for each one (preferring USDT, falling back to IRT) and handles
# Nobitex's scaled lots, so SHIB resolves to 1K_SHIBUSDT on its own.
#
# Blank lines and lines starting with # are ignored. Case does not matter.
# Anything that is not listed, or is listed for spot only, still appears in the
# dashboard's "not scanned" section with the reason — nothing is silently dropped.
#
# After editing, run:  ./run.sh setup     (re-runs discovery against this list)
#
# ---------------------------------------------------------------------------------
# SCOPE, changed 2026-08-22: this list is now the intersection of "tradeable on
# Tabdeal اهرم حرفه‌ای (Professional Leverage)" and "has a Toobit USDT perp".
#
# Why: the demo is being pointed at a future Tabdeal migration, and a track record
# built on coins Tabdeal does not list is not predictive of real trading. Tabdeal
# lists 33 futures symbols; 32 of them have a Toobit perp for candles/scanning.
# This cut the list from 74 coins to 32 and drops ~59% of the previous trade flow —
# expect materially fewer signals and a slower-accumulating sample.
#
# Verified 2026-08-22 against Tabdeal /r/fapi/v1/exchangeInfo (33 symbols, strict
# underscore format e.g. BTC_USDT) and Toobit's live contract list.
#
# Excluded deliberately:
#   GRAM   — on Tabdeal, but no Toobit USDT perpetual, so it cannot be scanned or
#            given candles. Listed here rather than dropped silently; revisit if a
#            Toobit contract appears or if candles ever come from elsewhere.
#
# Watch when reading results: PAXG and XAUT are gold-backed and track bullion, not
# crypto. They will behave very differently under the BTC-correlation and
# counter-trend gates than the rest of this list — treat their stats separately
# rather than pooling them with the altcoins.
# ---------------------------------------------------------------------------------

# Majors
BTC
ETH
BNB
SOL
XRP
ADA
DOGE
TRX
LTC
BCH

# Large / mid caps
AVAX
LINK
DOT
ATOM
NEAR
ICP
AAVE
SUI
TAO
HYPE
ZEC
XLM
HBAR
ALGO
POL
CAKE

# Meme / high-beta
SHIB
PEPE
WIF
FLOKI

# Gold-backed — see note above, do not pool with the altcoins when reviewing
PAXG
XAUT
